# ──────────────────────────────────────────────────────────────────────
# AI Pipeline — root .env
# Copy to .env and fill in your API keys
# NEVER commit .env to git
# ──────────────────────────────────────────────────────────────────────

# OpenAI (Script Engine + Topic Engine)
OPENAI_API_KEY=sk-proj-...

# Perplexity (Topic Engine — real-time research)
PERPLEXITY_API_KEY=pplx-...

# ElevenLabs (Voice Engine)
ELEVENLABS_API_KEY=...

# HeyGen (Media Engine — avatar video)
HEYGEN_API_KEY=...

# Pexels (Media Engine — B-roll stock footage)
PEXELS_API_KEY=...

# YouTube Data API v3 (Analytics Engine — views, likes)
YOUTUBE_API_KEY=...

# YouTube Analytics API OAuth2 (Analytics Engine — CPM, revenue)
# Get via: https://console.cloud.google.com/apis/credentials
YOUTUBE_OAUTH_TOKEN=

# Public URLs for Admin UI — must be reachable from the BROWSER
# In production: replace with real domain/IP
# In local dev: leave as localhost
TOPIC_ENGINE_PUBLIC_URL=http://localhost:3001
SCRIPT_ENGINE_PUBLIC_URL=http://localhost:3002
VOICE_ENGINE_PUBLIC_URL=http://localhost:3003
MEDIA_ENGINE_PUBLIC_URL=http://localhost:3004
ANALYTICS_ENGINE_PUBLIC_URL=http://localhost:3005
