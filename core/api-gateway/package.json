{
  "name": "@ai-pipeline/api-gateway",
  "version": "1.0.0",
  "description": "Single entry point: routes Admin UI requests to all microservices, aggregate health check",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "test": "jest"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/reply-from": "^9.6.0",
    "fastify": "^4.26.1",
    "axios": "^1.6.7",
    "dotenv": "^16.4.1",
    "pino": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
