# 🤖 Telegram Bot — EPIC I

Human-in-the-loop управление AI YouTube Pipeline прямо из Telegram.

## Порт
- Бот работает через long polling, HTTP-порт не нужен
- Связывается с API Gateway на порту **3100**
- Слушает Event Bus (Redis) на порту **6379**

## Команды

| Команда | Описание |
|---|---|
| `/start` | Главное меню с inline-кнопками |
| `/topics` | Список тем на одобрение |
| `/scripts` | Список скриптов на одобрение |
| `/stats` | Статус всех сервисов + аналитика |
| `/approve_topic <id>` | Одобрить тему |
| `/reject_topic <id>` | Отклонить тему |
| `/approve_script <id>` | Одобрить скрипт |
| `/reject_script <id>` | Отклонить скрипт |

## Push-уведомления (через Event Bus)

Бот автоматически присылает уведомления при:
- `analytics.hook_weak` — слабый хук (retention < 40%)
- `topic.pending_approval` — новая тема ждёт одобрения
- `script.pending_approval` — скрипт ждёт одобрения
- `localization.completed` — локализация готова
- `community.topic_exported` — тема из комментариев
- `hook_tester.winner_selected` — победитель A/B теста
- `analytics.niche_underperforming` — ниша недозарабатывает

## Настройка

```bash
cp .env.example .env
# Заполни TELEGRAM_BOT_TOKEN и ALLOWED_CHAT_IDS

npm install
npm run dev
```

## Получить токен

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Скопируй токен в `TELEGRAM_BOT_TOKEN`
4. Узнай свой chat ID через [@userinfobot](https://t.me/userinfobot)
5. Добавь ID в `ALLOWED_CHAT_IDS`
