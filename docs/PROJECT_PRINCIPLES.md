# Принципы проекта AI Pipeline

## ⚡ Главное правило

> **НЕТ НИКАКОГО MVP.**
> Есть только один конечный, прекрасно работающий продукт.

Мы не делаем «быстро и грязно», не режем углы «на потом»,
не выкатываем «заглушки» под предлогом итеративности.
Каждый модуль, который попадает в продакшн — работает
**идеально или не попадает вовсе.**

---

## 🏗️ Что это означает на практике

### 1. Архитектура — с первого раза
- Схема данных проектируется под финальный продукт, а не под «пока хватит».
- Контракты API (типы, DTO, ошибки) финальны на момент реализации модуля.
- Рефакторинг «потому что торопились» — недопустим.

### 2. Качество — встроено, а не добавлено
- Тесты (unit + integration + e2e) пишутся вместе с кодом, не «потом».
- Логирование, трейсинг, обработка ошибок — обязательная часть задачи.
- Код проходит ревью. Никаких прямых пушей в main без PR.

### 3. Каждый модуль — законченное произведение
- Модуль сдаётся тогда, когда он **полностью** реализует свою функцию.
- Нет «заглушек», нет «TODO в продакшне», нет hardcoded значений.
- Документация (README + API-примеры) — обязательная часть сдачи.

### 4. Скорость — через правильные решения, а не через срезание углов
- Лучше сделать меньше модулей, но каждый — безупречно.
- Порядок модулей определяется зависимостями, а не желанием «хоть что-то показать».
- Планирование — по готовности функции (Feature-Ready), а не по calendar deadline.

### 5. Пользователь видит только финальное состояние
- В репозитории не должно быть кода, который «пока не работает».
- Ветки feature/* — для разработки. Main — всегда стабильный, рабочий, полный.

---

## 📋 Чеклист готовности модуля к мержу

```
□ Функционал реализован полностью (нет «временных» заглушек)
□ Все поля схемы/DTO — финальные
□ Обработка всех edge-cases и ошибок
□ Unit + integration тесты написаны и проходят
□ Логирование всех ключевых шагов
□ Docker запускается без ошибок
□ API-документация / README обновлены
□ Код прошёл ревью Tech Lead
□ QA Engineer подтвердил работу руками
□ Product Owner одобрил поведение
```

---

## 🚫 Что запрещено

| Запрещено | Почему |
|-----------|--------|
| `// TODO: fix later` в продакшн-коде | «Потом» не наступает |
| Hardcoded API-ключи, URL, значения | Безопасность + гибкость |
| `any` в TypeScript | Теряем всю ценность типизации |
| Прямой пуш в `main` | Обходит ревью и QA |
| «Пока без тестов» | Без тестов — не работает, а «кажется что работает» |
| Заглушки в схеме БД | Миграции болезненны, схема — это контракт |
| «Выкатим, посмотрим» без мониторинга | Нет данных — нет решений |

---

## ✅ Стандарты кода

- **TypeScript strict mode** — всегда
- **Prisma** — единственный способ работы с БД
- **Fastify** — HTTP слой, строгая типизация роутов
- **Zod** — валидация входящих данных
- **Pino** — структурированные JSON-логи
- **Vitest** — unit/integration тесты
- **ESLint + Prettier** — форматирование, линт обязательны в CI

---

## 🎯 Единственная метрика успеха

> **Продукт работает — идеально, полностью, красиво.**
> Всё остальное — детали реализации.
